# Deployment Notes

The repository is connected to Vercel. Opening a pull request from `luxury-relaunch` should create a preview deployment if Vercel's Git integration is active.

After approval:

1. Merge the pull request into `main`.
2. Confirm Vercel production deployment succeeds.
3. Test the live domain on desktop and mobile.
4. Connect Wix/Square checkout before actively driving traffic.
